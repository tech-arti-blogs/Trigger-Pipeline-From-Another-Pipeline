# azure-pipelines.yml
name: FirstPipeline

trigger:
- main

resources:
  pipelines:
  - pipeline: tech-arti-blogs.Trigger-Pipeline-From-Another-Pipeline    # Name of the pipeline resource
    source: tech-arti-blogs.pipelines-java # Name of the pipeline referenced by the pipeline resource
    project: Configuring Pipeline # Required only if the source pipeline is in another project
    trigger: true # enable the trigger
    
  
pool:
  vmImage: ubuntu-latest
  
steps:
- script: This pipeline was set to be triggered after first pipeline completes.

